<h1>TasksController#index</h1>
<p>Find me in app/views/tasks_controller/index.html.erb</p>

<ul>
  <% @tasks.each do |r| %>
    <li><%= link_to r.name, task_path(r) %> (<%= resto.rating %>)</li>
  <% end %>
</ul>

<p><%= link_to "Add a task", new_task_path %></p>
